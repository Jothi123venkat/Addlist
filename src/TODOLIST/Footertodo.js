import React from 'react'

const Footertodo = (props) => {
  return (
    <div>

        <footer className='footer'>
            {props.length}
        </footer>

    </div>
  )
}

export default Footertodo